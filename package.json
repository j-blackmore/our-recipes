{
  "name": "our-recipes",
  "version": "1.0.0",
  "description": "Main package for our recipes. Contains the server",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\"",
    "start": "node server.js",
    "setup": "npm i && cd client && npm i",
    "heroku-postbuild": "cd client && npm install --only=dev && npm install && npm run build",
    "dev": "concurrently mongod \"nodemon server.js\" \"cd client && npm start\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "cloudinary": "^1.16.0",
    "concurrently": "^5.0.0",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "formidable": "^1.2.1",
    "mongoose": "^5.7.1"
  },
  "engines": {
    "node": "10.13.0"
  },
  "devDependencies": {
    "multer": "^1.4.2",
    "nodemon": "^2.0.2"
  }
}
